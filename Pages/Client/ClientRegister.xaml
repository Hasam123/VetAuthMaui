<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VetAuthMaui.ClientRegister"
             Title="Регистрация"
             BackgroundColor="#F6FAFF">

    <ScrollView>
        <VerticalStackLayout Padding="24,32,24,30" Spacing="16">
            <Label Text="Регистрация клиента"
                   TextColor="#17213A"
                   FontSize="28"
                   FontAttributes="Bold" />

            <Border BackgroundColor="White" Stroke="#DDEBF3" StrokeThickness="1" Padding="18">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="22" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="12">
                    <Entry x:Name="NameEntry"
                           Placeholder="Имя"
                           BackgroundColor="White"
                           TextColor="#222222"
                           PlaceholderColor="#9AA8B8" />

                    <Entry x:Name="PhoneEntry"
                           Placeholder="Телефон"
                           Keyboard="Telephone"
                           MaxLength="15"
                           BackgroundColor="White"
                           TextColor="#222222"
                           PlaceholderColor="#9AA8B8" />

                    <Entry x:Name="PasswordEntry"
                           Placeholder="Пароль"
                           IsPassword="True"
                           BackgroundColor="White"
                           TextColor="#222222"
                           PlaceholderColor="#9AA8B8" />

                    <Button Text="Создать аккаунт"
                            BackgroundColor="#30B878"
                            TextColor="White"
                            FontAttributes="Bold"
                            CornerRadius="26"
                            HeightRequest="52"
                            Clicked="Register_Click" />
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>









