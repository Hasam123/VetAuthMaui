<?xml version="1.0" encoding="utf-8" ?>
<!-- Экран входа администратора: логин, пароль и кнопка входа. -->
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VetAuthMaui.AdminLogin"
             Title="Вход администратора"
             BackgroundColor="#F6FAFF"
             Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Padding="24,28,24,30" Spacing="18">
            <Label
                Text="Вход администратора"
                TextColor="#17213A"
                FontSize="26"
                FontAttributes="Bold" />

            <Label
                Text="Введите логин и пароль для управления заявками и услугами."
                TextColor="#657084"
                FontSize="15"
                LineBreakMode="WordWrap" />

            <Border
                BackgroundColor="White"
                Stroke="#DDEBF3"
                StrokeThickness="1"
                Padding="16"
                Margin="0,8,0,0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="22" />
                </Border.StrokeShape>

                <VerticalStackLayout Spacing="14">
                    <Label Text="Логин" TextColor="#17213A" FontAttributes="Bold" />

                    <Entry
                        x:Name="LoginEntry"
                        Placeholder="admin"
                        Keyboard="Email"
                        ClearButtonVisibility="WhileEditing"
                        TextColor="#222222"
                        PlaceholderColor="#9AA8B8" />

                    <Label Text="Пароль" TextColor="#17213A" FontAttributes="Bold" />

                    <Entry
                        x:Name="PasswordEntry"
                        Placeholder="Введите пароль"
                        IsPassword="True"
                        ClearButtonVisibility="WhileEditing"
                        TextColor="#222222"
                        PlaceholderColor="#9AA8B8" />

                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="10">
                        <CheckBox
                            x:Name="ShowPasswordCheckBox"
                            Color="#12A7A7"
                            CheckedChanged="ShowPassword_Changed" />

                        <Label
                            Grid.Column="1"
                            Text="Показать пароль"
                            TextColor="#17213A"
                            VerticalTextAlignment="Center" />
                    </Grid>

                    <Button
                        Text="Войти"
                        BackgroundColor="#FF8A5B"
                        TextColor="White"
                        FontAttributes="Bold"
                        CornerRadius="26"
                        HeightRequest="52"
                        Clicked="Login_Click" />
                </VerticalStackLayout>
            </Border>

            <Label
                x:Name="MessageLabel"
                Text=""
                TextColor="#30B878"
                FontSize="14"
                LineBreakMode="WordWrap" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>








